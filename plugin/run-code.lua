-- Initialize the plugin with default settings if not already done.
-- This allows the plugin to work out-of-the-box without explicit setup() call,
-- matching the behavior of the original Vim plugin.
require('run-code').setup({})
